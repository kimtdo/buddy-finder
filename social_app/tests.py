from django.test import TestCase
from social_app.models import Profile
from social_app.views import ProfileView

# Create your tests here.

#class ProfileTests(TestCase):
 #   def setUp(self):
  #      Profile.objects.create(name = "test", bio = "bio", gender = "gender")

   # def test_profile_fields(self): #test when you submit full profile form
    #    t = Profile.objects.get(name = "test")
     #   p = Profile
      #  p.name = "test"
       # p.bio = "bio"
        #p.gender = "gender"
        #self.assertTrue(p.name == t.name)
        #self.assertTrue(p.bio == t.bio)
        #self.assertTrue(p.gender == t.gender)

